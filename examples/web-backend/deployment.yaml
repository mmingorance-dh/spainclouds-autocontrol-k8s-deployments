apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-backend
  namespace: rojo
  labels:
    servicio: web-backend
    version: v1
    equipo: rojo
spec:
  replicas: 1
  selector:
    matchLabels:
      servicio: web-backend
      version: v1
      equipo: rojo
  strategy: {}
  template:
    metadata:
      labels:
        servicio: web-backend
        version: v1
        equipo: rojo
    spec:
      containers:
      - image: nginx
        name: nginx
        resources: {}
